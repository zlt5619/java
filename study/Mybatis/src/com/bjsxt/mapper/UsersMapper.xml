<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bjsxt.mapper.UsersMapper">
<!--    查询所有用户-->
    <select id="selectUsersAll" resultType="com.bjsxt.pojo.Users">
        select * from users
    </select>
<!--    根据用户id查询用户-->
    <select id="selectUserById" parameterType="int" resultType="com.bjsxt.pojo.Users">
       select * from users where userid=#{suibian}
    </select>
    <!--添加用户-->
    <insert id="insertUsers" >
        insert into users value(default ,#{username},#{usersex})
    </insert>
<!--    删除用户-->
    <delete id="deleteUsersById">
        delete from users where userid = #{userid}
    </delete>
<!--    更新操作,两步-->
<!--    1\查询相应用户-->
<!--    2\进行更新处理-->
    <!--预更新用户的查询-->
    <select id="selectUserById2" resultType="com.bjsxt.pojo.Users">
        select * from users where userid = ${userid}
    </select>
<!--    更新用户-->
    <update id="updateUsersById">
        update users set username=#{username},usersex=#{usersex} where userid=#{userid};
    </update>

<!--    根据用户名和用户性别查询参数-->
<!--    在typealias中，设定了user类为u-->
    <select id="selectUserByNameAndSex" resultType="u">
        select * from users where username=#{name} and usersex=#{sex}
    </select>
</mapper>
